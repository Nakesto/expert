import { Flex, Box, Image } from "@chakra-ui/react";
import { motion } from "framer-motion";

export const FlexMotion = motion(Flex);

export const BoxMotion = motion(Box);

export const ImgMotion = motion(Image);
